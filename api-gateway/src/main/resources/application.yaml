server:
  port: 8080

spring:
  application:
    name: api-gateway

  main:
    web-application-type: reactive

  cloud:
    gateway:
      routes:
        - id: auth-service
          uri: http://auth-service:8081
          predicates:
            - Path=/api/auth/**, /api/admin/**, /api/roles/**

        - id: reservation_service
          uri: http://reservation-service:8082
          predicates:
            - Path=/api/v1/reservas/**, /api/v1/viajeros/**, /api/v1/confirmaciones/**

        - id: tourist_catalog_service
          uri: http://tourist-catalog-service:8083
          predicates:
            - Path=/categorias/**, /paquetes/**, /proveedores/**

      globalcors:
        corsConfigurations:
          "[/**]":
            allowedOrigins:
              - "http://localhost:4200"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
            allowedHeaders:
              - "*"
            allowCredentials: true

      default-filters:
        - name: DedupeResponseHeader
          args:
            # <--- Debe estar dentro del filtro
            name: Access-Control-Allow-Origin Access-Control-Allow-Credentials
            strategy: RETAIN_UNIQUE
app:
  jwt:
    secret: oH+9q7wdTI5MPQmt5wRelMhO70B6FmEGOanhAPEAGbM=
    expiration: 900000
