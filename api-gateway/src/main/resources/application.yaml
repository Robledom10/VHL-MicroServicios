server:
  port: 8080

spring:
  application:
    name: api-gateway

  main:
    web-application-type: reactive

  cloud:
    gateway:
      routes:
        # =========================================================
        # AUTH SERVICE
        # =========================================================
        - id: auth-service
          uri: http://auth-service:8081
          predicates:
            - Path=/api/auth/**,/api/admin/**,/api/roles/**

        # =========================================================
        # RESERVATION SERVICE
        # =========================================================
        - id: reservation-service
          uri: http://reservation-service:8082
          predicates:
            - Path=/api/v1/reservas/**,/api/v1/viajeros/**,/api/v1/confirmaciones/**

        # =========================================================
        # TOURIST CATALOG SERVICE
        # =========================================================
        - id: tourist-catalog-service
          uri: http://tourist-catalog-service:8083
          predicates:
<<<<<<< Updated upstream
            - Path=/api/categorias/**,/api/paquetes/**,/api/proveedores/**
=======
            - Path=/api/paquetes/**,/api/proveedores/**
>>>>>>> Stashed changes

        # =========================================================
        # GALLERY SERVICE
        # =========================================================
        - id: gallery-service
          uri: http://gallery-service:8084
          predicates:
            - Path=/api/media/**

        # =========================================================
<<<<<<< Updated upstream
        # FINANCIAL SERVICE
        # =========================================================

        - id: financial-service
          uri: http://financial-service:8085
          predicates:
            - Path=/api/payments/**,/api/webhook/**

      # =========================================================
      # CORS
      # =========================================================

        - id: operation_service
          uri: http://operation-service:8084
=======
        # OPERATION SERVICE
        # =========================================================
        - id: operation-service
          uri: http://operation-service:8085
>>>>>>> Stashed changes
          predicates:
            - Path=/api/v1/operaciones/**

      # =========================================================
      # CORS
      # =========================================================
      globalcors:
        corsConfigurations:
          "[/**]":
            allowedOrigins:
              - "http://localhost:4200"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
            allowedHeaders:
              - "*"
            allowCredentials: true

      # =========================================================
      # DEFAULT FILTERS
      # =========================================================
      default-filters:
        - name: DedupeResponseHeader
          args:
            name: Access-Control-Allow-Origin Access-Control-Allow-Credentials
            strategy: RETAIN_UNIQUE

# =========================================================
# JWT
# =========================================================
app:
  jwt:
    secret: ${JWT_SECRET}
    expiration: ${JWT_EXPIRATION}
