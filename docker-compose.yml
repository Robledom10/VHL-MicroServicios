services:
  db-auth:
    image: mysql:8
    container_name: db-auth
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${AUTH_DB_NAME}
    ports:
      - "${AUTH_DB_PORT}:3306"
    volumes:
      - auth_data:/var/lib/mysql
      - ./Docker/auth_service.sql:/docker-entrypoint-initdb.d/auth_service.sql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 20s
    networks:
      - microservices-net

  db-reservation:
    image: mysql:8
    container_name: db-reservation
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${RESERVATION_DB_NAME}
    ports:
      - "${RESERVATION_DB_PORT}:3306"
    volumes:
      - reservation_data:/var/lib/mysql
      - ./Docker/reservation_service.sql:/docker-entrypoint-initdb.d/reservation_service.sql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 20s
    networks:
      - microservices-net

  auth-service:
    build: ./auth-service
    container_name: auth-service
    restart: always
    depends_on:
      db-auth:
        condition: service_healthy
    env_file:
      - .env
    environment:
      SPRING_PROFILES_ACTIVE: dev
      SPRING_DATASOURCE_URL: jdbc:mysql://db-auth:3306/${AUTH_DB_NAME}?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
      SPRING_DATASOURCE_USERNAME: ${MYSQL_USER}
      SPRING_DATASOURCE_PASSWORD: ${MYSQL_ROOT_PASSWORD}

      APP_JWT_SECRET: ${JWT_SECRET}
      APP_JWT_EXPIRATION: ${JWT_EXPIRATION}
    ports:
      - "${AUTH_SERVICE_PORT}:8081"
    networks:
      - microservices-net

  reservation-service:
    build: ./reservation-service
    container_name: reservation-service
    restart: always
    depends_on:
      db-reservation:
        condition: service_healthy
    env_file:
      - .env

    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://db-reservation:3306/${RESERVATION_DB_NAME}?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
      SPRING_DATASOURCE_USERNAME: ${MYSQL_USER}
      SPRING_DATASOURCE_PASSWORD: ${MYSQL_ROOT_PASSWORD}
    ports:
      - "${RESERVATION_SERVICE_PORT}:8082"
    networks:
      - microservices-net

  api-gateway:
    build: ./api-gateway
    container_name: api-gateway
    restart: always
    depends_on:
      - auth-service
      - reservation-service

    env_file:
      - .env

    environment:
      APP_JWT_SECRET: ${JWT_SECRET}
      APP_JWT_EXPIRATION: ${JWT_EXPIRATION}
    ports:
      - "${API_GATEWAY_PORT}:8080"
    networks:
      - microservices-net

networks:
  microservices-net:

volumes:
  auth_data:
  reservation_data:
