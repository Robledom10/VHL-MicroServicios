server:
  port: 8087

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gestion_documental_db
    username: root
    password: 

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB